# Contributing

## Development

1. Install Hugo and Node.js
2. Run `make dev` to start the server
3. Edit files in `content/` and `data/` directories

## Content Rules

- All professional data must come from `profile-data/` directory
- Keep content professional and metrics-focused
- Spell check all content before committing
